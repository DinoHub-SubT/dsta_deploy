- pwd
- 'export SRC_DIR=$PWD'

- perception:
  - cd $SRC_DIR/docker/scripts/
  - cuda9:
    - cuda:
      - ./build.bash --env tensorflow/cuda --force
    - objdet:
      - ./build.bash --env tensorflow/objdet --force
    - ros:
      - ./build.bash --env tensorflow/ros --force

# add alias to go to docker scripts