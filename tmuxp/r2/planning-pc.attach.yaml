session_name: r2_planning_pc_start

# Add commands to bash history
suppress_history: true

start_directory: /home/$USER/deploy_ws/src/

# This is where we list all the stuff we want to run
windows:
  - window_name: recorder
    focus: false
    layout: main-horizontal
    panes:
      - shell_command:
        - ssh ugv2@ugv2
        - docker-join
        - tmux a -t launch_record

  - window_name: main
    focus: true
    layout: main-horizontal
    panes:
      # enter the docker container on planning-pc
      - shell_command:  
        - ssh ugv2@ugv2
        - docker-join
        - tmux a -t launch_planning
      - shell_command:
        - ssh ugv2@ugv2
        - docker-join
        - tmux a -t launch_hardware
  
  - window_name: docker shell
    focus: false
    layout: main-horizontal
    panes:
      # enter the docker container on planning-pc
      - shell_command:
        - ssh ugv2@ugv2
        - docker-join
        - tmux a -t ros_shell