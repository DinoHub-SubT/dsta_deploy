session_name: r2_stop_all

# Add commands to bash history
suppress_history: true

start_directory: /home/$USER/deploy_ws/src/

# This is where we list all the stuff we want to run
windows:
  - window_name: r2 stop
    focus: false
    layout: main-horizontal
    panes:
      - shell_command:
        # - ./deployer -s r2.nuc.launch.stop r2.planning-pc.launch.stop
        - ./deployer -s r2.nuc.docker.stop
        - ./deployer -s r2.planning-pc.docker.stop
