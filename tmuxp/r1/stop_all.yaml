session_name: r1_stop_all

# Add commands to bash history
suppress_history: true

start_directory: /home/$USER/deploy_ws/src/

# This is where we list all the stuff we want to run
windows:
  - window_name: r1 stop
    focus: false
    layout: main-horizontal
    panes:
      - shell_command:
        # - ./deployer -s r1.nuc.launch.stop r1.planning-pc.launch.stop
        - ./deployer -s r1.nuc.docker.stop
        - ./deployer -s r1.planning-pc.docker.stop
